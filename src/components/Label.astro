---
interface Props {
  label: string;
  class?: string;
}

const { label, ...props } = Astro.props;
---

<span {...props}>{label}</span>

<style>
  span {
    font-size: 2.625rem;
    font-style: normal;
    line-height: normal;
  }
</style>
